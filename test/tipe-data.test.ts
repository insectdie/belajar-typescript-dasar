describe('Data Type',  () => {
    it('should must declare',  () => {

        const name: string = "Andry Halomoan Ompusunggu";
        const balance: number = 100000;
        const isVip: boolean = true;
        
        console.info(name);
        console.info(balance);
        console.info(isVip);

    })
})